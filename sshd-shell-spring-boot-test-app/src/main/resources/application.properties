logging.level.sshd.shell=DEBUG

management.endpoint.shutdown.enabled=true

sshd.shell.enabled=true
sshd.shell.text.color=BLUE
sshd.shell.prompt.color=RED
sshd.shell.prompt.title=server

### Enable configuration below for authentication/authorization via spring-security
#sshd.shell.auth.authType=AUTH_PROVIDER

### Enable configuration below for simple authentication/authorization
sshd.shell.password=admin

spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.protocol=smtps
# Enable and replace username/password below to test with gmail (ensure 'less secure app' setting is turned on)
#spring.mail.username=
#spring.mail.password=
